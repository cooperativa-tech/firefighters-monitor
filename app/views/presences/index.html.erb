<div class="Presences">
  <div class="Presences-list" data-controller="presences" data-presences-current_user_id=<%= current_user.id %>>
    <% @users.each do |user| %>
      <button
        class="Presences-user Presences-user--<%= user.status %>"
        data-user_id="<%= user.id %>"
        data-action="click->presences#toggleStatus contextmenu->presences#toggleDutyType"
      >
        <span><%= user.username %></span>

        <% if user.duty_type.present? %>
          <div class="Presences-userDutyType">
            <%= User.human_enum_name(:duty_type, user.duty_type) %>
          </div>
        <% end %>

        <div class="Presences-userStatus">
          <% unless user.unavailable? %>
            <%= User.human_enum_name(:status, user.status) %>
          <% end %>
        </div>
      </button>
    <% end %>
  </div>

  <div class="Presences-statuses">
    <h2><%= User.human_enum_name(:status, :available) %></h2>

    <% @available_users.each do |user| %>
      <p><%= user.username %></p>
    <% end %>

    <h2><%= User.human_enum_name(:status, :service) %></h2>

    <% @service_users.each do |user| %>
      <p><%= user.username %></p>
    <% end %>

    <h2><%= User.human_enum_name(:duty_type, :picket) %></h2>

    <% @picket_users.each do |user| %>
      <p><%= user.username %></p>
    <% end %>

    <h2><%= User.human_enum_name(:duty_type, :elac) %></h2>

    <% @elac_users.each do |user| %>
      <p><%= user.username %></p>
    <% end %>

    <h2><%= User.human_enum_name(:duty_type, :eip) %></h2>

    <% @eip_users.each do |user| %>
      <p><%= user.username %></p>
    <% end %>
  </div>
</div>
